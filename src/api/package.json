{
  "name": "@kpx/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "db:ensure": "[ -d ../../db/node_modules ] || npm --prefix ../../db ci",
    "db:build": "npm run db:ensure --silent && npm --prefix ../../db run build --silent",
    "predev": "npm run db:build --silent",
    "dev": "tsx watch src/main.ts",
    "prebuild": "npm run db:build --silent",
    "build": "tsc -p tsconfig.build.json",
    "pretypecheck": "npm run db:build --silent",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "pretest": "npm run db:build --silent",
    "test": "vitest run"
  },
  "dependencies": {
    "@kpx/db": "file:../../db",
    "fastify": "5.12.3"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "22.20.1",
    "eslint": "10.10.0",
    "globals": "17.12.0",
    "tsx": "4.23.13",
    "typescript": "5.9.3",
    "typescript-eslint": "8.69.0",
    "vitest": "5.0.0"
  }
}
